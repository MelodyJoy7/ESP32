/* 5/2/2023
   This will the updated version as an update to be sent to ESP32.
*/

// Runs once 
void setup() {
    // Initialize GIOP2 (blue LED) as output
    pinMode(2, OUTPUT);
}

// Loops
void loop() {
    digitalWrite(2, HIGH);
    delay(500);
    digitalWrite(2, LOW);
    delay(500);
}